[project]
name = "validation-core"
version = "0.1.0"
description = "Small runtime validation primitives for reusable Python packages"
readme = "README.md"
authors = [
    { name = "Thlamz", email = "thlamenza@hotmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["uv_build>=0.8.18,<0.9.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
